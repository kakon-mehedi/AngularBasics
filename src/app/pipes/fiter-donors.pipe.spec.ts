import { FiterDonorsPipe } from './filter-donors.pipe';

describe('FiterDonorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterDonorsPipe();
    expect(pipe).toBeTruthy();
  });
});
